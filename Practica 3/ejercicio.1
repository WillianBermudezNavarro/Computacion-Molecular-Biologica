from Bio import SeqIO
sequences = SeqIO.parse("C:\Willian UNSA\Molecular\programa\P21333.fasta.txt", "fasta")#Directorio del archivo
for record in sequences:
    data1 = str(record.seq.upper()) # the fasta file just have one sequence
sequences = SeqIO.parse("C:\Willian UNSA\Molecular\programa\Q8BTM8.fasta.txt", "fasta")#Directorio del archivo
for record in sequences:
    data2 = str(record.seq.upper()) # the fasta file just have one sequence
print(data1)
print(data2)

